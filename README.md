# Chess-App
A Simple web chess app

## Environment Setup
1. In the `client` folder, copy `.env.example` to `.env.local` (or `.env`).
2. Populate all `VITE_FIREBASE_*` values with your Firebase project credentials.

## Cleanup
Remove the obsolete `src/firebase.jsx` file to avoid duplicate configuration.
